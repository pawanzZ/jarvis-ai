{
  "name": "jarvis-ai",
  "version": "0.1.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && node scripts/copy-assets.js",
    "test": "npm run build && node scripts/test-modules.js",
    "dev": "npm run build && electron dist/main.js",
    "start": "electron dist/main.js"
  },
  "devDependencies": {
    "electron": "^44.0.0",
    "potrace": "^2.1.8",
    "typescript": "^5.4.0"
  },
  "allowScripts": {
    "electron@30.5.1": true
  }
}
